streamlit==1.25.0   # Replace with the latest version if different
pandas==1.5.3       # Replace with the latest version if different
mysql-connector-python==8.0.33
matplotlib==3.7.2
Pillow==10.0.1      # PIL module
requests==2.31.0    # For handling HTTP requests
google-generativeai==0.1.0  # Replace with the actual package version or equivalent
